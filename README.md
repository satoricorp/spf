# Hypervisor

A local macOS Tauri app for monitoring coding agent sessions.

## Development

Install dependencies:

```sh
npm install
```

Run the desktop app:

```sh
npm run tauri dev
```

Run the frontend build:

```sh
npm run build
```

Check the Rust/Tauri side:

```sh
cd src-tauri
cargo check
```
